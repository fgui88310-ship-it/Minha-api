import express from "express";
import { tunaSearchController } from "../../controllers/tuna.controller.js";

const router = express.Router();

router.get("/", tunaSearchController);

export default router;