const ReceitaModule = {
  searchUrl(q) {
    const searchTerm = encodeURIComponent(q.replace(/\s+/g, '+').toLowerCase());
    return `https://cybercook.com.br/search?q=${searchTerm}&is_premium=true&calorias=0&custo=0&prep=0`;
  }
};